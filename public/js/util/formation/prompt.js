define('prompt', ["jQuery", "underscore", "util", "text!prompt.html"], function ( jquery, _, Util, html ){	var util = new Util();	var Prompt = function (){};	var timer = null;	Prompt.prototype = {		constructor : Prompt,		show : function ( msg ){			this.hide();			util.renderTemplate({				template : html,				wrapper : $(document.body),				method : "append",				data : {					"message" : msg				}			});			timer && clearTimeout(timer);			timer = setTimeout(this.hide,2000);		},		hide : function (){			$(".message").remove();		}	};	return Prompt;});